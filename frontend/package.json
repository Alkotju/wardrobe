{
  "name": "wardrobe-frontend-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Tests for the wardrobe vanilla-JS SPA. The app itself is served statically by nginx — this package only carries dev dependencies for Jest.",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
